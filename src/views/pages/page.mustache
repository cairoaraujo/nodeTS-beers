{{>partials/header}}
<section class="banner" style="background-image: url('images/{{banner.background}}')">{{banner.title}}</section>
<img href= '{{& background}}'>
<div class="container list">

    {{#list}}
        <div class="item">
        <img src="images/{{image}}" class="item--image" />
        <div class="item--name">{{name}}</div>
        <div class="item--type">Tipo: {{type}}</div>
        <div class="item--color">Cor: {{color}}</div>
        <div class="item--color">Lançada em {{releasedDate}}</div>
    </div>
    {{/list}}

</div>

{{^list}}
    <div class= "container no-list">
        <h2>Nenhuma cerveja encontrada.</h2>
    </div>
{{/list}}

{{>partials/footer}}